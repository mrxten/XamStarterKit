using System;
namespace XamStarterKit {
    public enum NavigationMode {
        Normal,
        Modal,
        Custom,
        Root,
        PopUp
    }

    public enum KitPageState {
        Clean,
        Loading,
        Empty,
        Normal,
        Error
    }
}
